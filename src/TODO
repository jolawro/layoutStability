Optimized carousel  
Sticky footer problem                                                                                   DONE
Article                                                                                                 DONE    
LQUIF
cookies - linked in 
gallery of squares                                                                                      DONE
preloaders of images
https://web.dev/native-lazy-loading
    intersection observer https://developers.google.com/web/updates/2016/04/intersectionobserver
    implement resize, scroll & orientationchange
SVG distracted promo image                                                                              DONE
webm video format istead of gif

const imageObserver = new IntersectionObserver((entries, imgObserver) => {
    entries.forEach((entry) => {
        if(entry.isIntersecting) {
            const lazyImage = entry.target
            lazyImage.src = lazyImage.dataset.src
        }
    })
});

show how its done, not only talk how you should do it